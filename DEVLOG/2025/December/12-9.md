# SageXR Devlog: ❄️12-9-2025❄️

View the repo: https://github.com/TheSevenSages/SageXR

## Goals for Today

###### *What are the most critical tasks I plan to tackle today?*

1. Continue building out abstraction
2. Learn how to send mesh data so the headset's environment scans can be sent to the desktop.

## Accomplishments

###### *What did I actually complete?*

* Now users can send meshes back and forth.
  *  Created a new DataStructure namespace that stores all of the classes needed for the backend message structure.
* Add simple mesh serializer.
  * I had to build both a mesh viewer and sender to actually test the ability to send meshes back and forth.
* Build out abstraction layers
  * Messenger
    * Front line layer for the rest of the codebase.

## Challenges & Blockers

###### *What problems did I run into? Even small frustrations are worth noting.*

* Json serializers have a hard time with Vector3 since .normalize returns a Vector3 and some serializers view that as an infinite loop.

## Learnings & Key Takeaways

###### *What did I learn today? Could be a new skill, trick, process, or even a fun fact!*

* There are a lot of Unity functions that cannot be called anywhere except the main thread (i.e not in a callback response to a server message or anything like that to name an example)
  * This includes instantiate, which meant I had to redo how I created the meshes when a MESHES message is received.
    * Now I have the message get added to a list that gets processed during Update.
      * TODO: Transfer that over to the Messenger so invokes are done on the main thread.

## Plan for Next Time

###### *Based on today's progress, what are the next steps?*

1. Connect this new mesh transfer protocol to the AVPs room sensing tech to send meshes of the room layout between devices.
   1. Planes > full meshes

## Notes

###### *Other thoughts that I felt needed to be written down.*

Nothing today!